#Paffs drivers
import os

Import('envGlobal')

print("SIMUDRIVER")

envGlobal.Append(LIBS=[
	'simuDriver',
	'simu',
])

files  = ['simu.cpp', 'yaffs_ecc.cpp']
library = envGlobal.StaticLibrary('simuDriver', files)

envGlobal.Install('$BUILDPATH/lib', library)
